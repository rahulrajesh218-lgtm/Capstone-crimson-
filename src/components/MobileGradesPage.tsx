import { Calculator, CheckCircle2, GraduationCap, Plus, Trash2 } from "lucide-react";

type Assessment = { id: number; name: string; score: number; total: number; factor: number };
type Course = { id: number; name: string; goal: number; assessments: Assessment[] };
type Props = { courses: Course[]; overall: number | null; atGoal: number; onAddCourse: () => void; onAddMark: () => void; onGoalChange: (courseId: number, goal: number) => void; onDeleteCourse: (id: number) => void; onDeleteAssessment: (courseId: number, assessmentId: number) => void };

function average(course: Course) {
  const weight = course.assessments.reduce((sum, item) => sum + Math.max(0, item.factor), 0);
  if (!weight) return null;
  return course.assessments.reduce((sum, item) => sum + (item.total > 0 ? item.score / item.total * 100 : 0) * Math.max(0, item.factor), 0) / weight;
}

export function MobileGradesPage({ courses, overall, atGoal, onAddCourse, onAddMark, onGoalChange, onDeleteCourse, onDeleteAssessment }: Props) {
  return <div className="space-y-4 md:hidden"><header><p className="text-sm font-semibold text-indigo-700">Academics</p><h2 className="mt-1 text-2xl font-semibold">Grade tracker</h2><p className="mt-1 text-sm text-zinc-500">Keep a simple estimate of where each course stands.</p></header><div className="grid grid-cols-3 gap-2"><div className="rounded-2xl border border-zinc-200 bg-white p-3"><GraduationCap className="h-5 w-5 text-indigo-600" /><p className="mt-3 text-2xl font-semibold">{courses.length}</p><p className="text-xs text-zinc-500">Courses</p></div><div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-3"><CheckCircle2 className="h-5 w-5 text-emerald-600" /><p className="mt-3 text-2xl font-semibold">{atGoal}</p><p className="text-xs text-zinc-500">At goal</p></div><div className="rounded-2xl border border-violet-200 bg-white p-3"><Calculator className="h-5 w-5 text-violet-600" /><p className="mt-3 truncate text-2xl font-semibold">{overall === null ? "—" : `${overall.toFixed(1)}%`}</p><p className="text-xs text-zinc-500">Average</p></div></div><div className="grid grid-cols-2 gap-3"><button onClick={onAddCourse} className="flex min-h-12 items-center justify-center gap-2 rounded-2xl bg-[#02031c] px-3 font-semibold text-white"><Plus className="h-5 w-5" />Course</button><button onClick={onAddMark} disabled={!courses.length} className="flex min-h-12 items-center justify-center gap-2 rounded-2xl border border-zinc-300 bg-white px-3 font-semibold text-zinc-800 disabled:opacity-40"><Plus className="h-5 w-5" />Mark</button></div>{courses.length ? <div className="space-y-3">{courses.map(course => { const value = average(course); const onTrack = value !== null && value >= course.goal; return <article key={course.id} className="rounded-3xl border border-zinc-200 bg-white p-4 shadow-sm"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><h3 className="break-words text-xl font-semibold">{course.name}</h3><p className={`mt-1 text-sm font-semibold ${onTrack ? "text-emerald-600" : "text-orange-600"}`}>{value === null ? "Add your first mark" : onTrack ? "On track" : `${(course.goal - value).toFixed(1)}% below goal`}</p></div><button onClick={() => onDeleteCourse(course.id)} className="min-h-11 min-w-11 rounded-xl border border-zinc-200 text-red-600" aria-label={`Delete ${course.name}`}><Trash2 className="mx-auto h-4 w-4" /></button></div><div className="mt-4 grid grid-cols-2 gap-2"><div className="rounded-2xl bg-indigo-50 p-3"><p className="text-xs font-semibold uppercase text-indigo-600">Estimate</p><p className="mt-1 text-2xl font-semibold">{value === null ? "—" : `${value.toFixed(1)}%`}</p></div><label className="rounded-2xl bg-zinc-50 p-3"><span className="text-xs font-semibold uppercase text-zinc-500">Goal</span><div className="mt-1 flex items-center"><input type="number" min="0" max="100" value={course.goal} onChange={event => onGoalChange(course.id, Number(event.target.value))} className="min-w-0 flex-1 bg-transparent text-2xl font-semibold outline-none" /><span>%</span></div></label></div><div className="mt-4 space-y-2">{course.assessments.map(item => <div key={item.id} className="flex items-center gap-3 rounded-2xl border border-zinc-200 p-3"><div className="min-w-0 flex-1"><p className="truncate font-semibold">{item.name}</p><p className="text-sm text-zinc-500">{item.score}/{item.total} · {item.total > 0 ? (item.score / item.total * 100).toFixed(1) : "0.0"}% · Factor {item.factor}</p></div><button onClick={() => onDeleteAssessment(course.id, item.id)} className="min-h-11 min-w-11 rounded-xl text-red-600" aria-label={`Delete ${item.name}`}><Trash2 className="mx-auto h-4 w-4" /></button></div>)}{!course.assessments.length && <p className="rounded-2xl border border-dashed border-zinc-300 p-4 text-center text-sm text-zinc-500">No marks added yet.</p>}</div></article>})}</div> : <div className="rounded-3xl border border-dashed border-zinc-300 bg-white p-8 text-center"><GraduationCap className="mx-auto h-9 w-9 text-indigo-500" /><h3 className="mt-3 font-semibold">No courses yet</h3><p className="mt-1 text-sm text-zinc-500">Add a course to start tracking your marks.</p></div>}</div>;
}
